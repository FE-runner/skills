---
phase: 02-selective-merge
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/source-parser.ts
  - src/cli.ts
autonomous: true
requirements:
  - FEAT-01
  - FEAT-02
  - SEC-01
  - BUG-03

must_haves:
  truths:
    - "source-parser.ts 支持 branch ref 格式（owner/repo#branch）"
    - "source-parser.ts 包含 sanitizeSubpath() 防路径遍历函数"
    - "source-parser.ts 保留 BMC 的 Market 优先解析逻辑和所有 market/cos 相关函数"
    - "cli.ts 包含 --json flag 支持"
    - "cli.ts 保留 BMC 的品牌常量引用和 Market 集成"
    - "合并提交已创建，工作区干净"
  artifacts:
    - path: "src/source-parser.ts"
      provides: "源解析器（含 BMC market 类型 + 上游 branch ref + sanitizeSubpath）"
      contains: "sanitizeSubpath"
    - path: "src/cli.ts"
      provides: "CLI 主入口（含 BMC 品牌 + 上游 --json + update-source 集成）"
      contains: "json"
  key_links:
    - from: "src/source-parser.ts"
      to: "src/types.ts"
      via: "ParsedSource 类型（含 market 类型）"
      pattern: "ParsedSource"
    - from: "src/cli.ts"
      to: "src/update-source.ts"
      via: "buildUpdateInstallSource import"
      pattern: "update-source"
    - from: "src/cli.ts"
      to: "src/branding.ts"
      via: "品牌常量引用"
      pattern: "BIN_NAME\\|NPX_CMD\\|SKILLS_SITE"
---

<objective>
解决 source-parser.ts 和 cli.ts 的高复杂度合并冲突，完成合并提交。

Purpose: 这两个文件是双方改动量最大的文件（各 100+ 行变更），需要逐段审查手动合并。source-parser.ts 要引入 branch ref 支持和安全修复，cli.ts 要引入 --json flag 和 update-source 集成，同时保留所有 BMC 定制。
Output: 干净的合并提交，工作区无未解决冲突。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-selective-merge/02-01-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/research/FEATURES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 解决 source-parser.ts 高冲突合并（branch ref + sanitizeSubpath + BMC market 逻辑）</name>
  <files>src/source-parser.ts</files>
  <read_first>
    src/source-parser.ts,
    src/types.ts
  </read_first>
  <action>
**背景（陷阱 #2 警告）：** source-parser.ts 是本次合并最复杂的文件。上游几乎重写了 `parseSource()` 函数（+96 行），新增了 fragment ref 解析、sanitizeSubpath()、github: 协议等。BMC 在同一函数中插入了 Market 优先级解析。**不能机械合并，需要理解双方架构后手动融合。**

**步骤 1: 理解当前冲突状态**
```bash
# 查看冲突标记数量
grep -c "<<<<<<<" src/source-parser.ts
# 查看完整文件（含冲突标记）
cat src/source-parser.ts
```

**步骤 2: 获取三方参考版本**
```bash
# 上游版本（theirs）
git show upstream/main:src/source-parser.ts > /tmp/source-parser-upstream.ts
# BMC 合并前版本（ours, 即 HEAD 在合并前的版本）
git show HEAD:src/source-parser.ts > /tmp/source-parser-bmc.ts 2>/dev/null || git show MERGE_HEAD~1:src/source-parser.ts > /tmp/source-parser-bmc.ts 2>/dev/null
```

**步骤 3: 手动融合策略**

按照以下优先级逐块处理冲突区域：

**(a) 保留 BMC 的 Market 解析逻辑（最高优先级）：**
- `isMarketInstallCommand()` 函数 — 完整保留
- `isBareSkillName()` 函数 — 完整保留
- `parseMarketInstallCommand()` 函数 — 完整保留
- `parseSource()` 中的 Market 优先级分支（先尝试 Market，再回退 GitHub）— 完整保留
- ParsedSource 类型中的 `market` 类型和 `marketQuery` 等字段 — 完整保留
- COS URL 相关解析逻辑 — 完整保留

**(b) 从上游引入安全修复（per SEC-01）：**
- `sanitizeSubpath()` 函数 — 完整引入。该函数阻止 `..` 段逃逸。在 BMC 版本中找到合适位置添加（通常在文件顶部的辅助函数区域）。
- 在 `parseSource()` 中对 subpath 参数调用 `sanitizeSubpath()` — 引入

**(c) 从上游引入 branch ref 支持（per FEAT-01）：**
- `parseFragmentRef()` 或类似函数 — 引入。解析 `owner/repo#branch` 格式中的 `#branch` 部分。
- `looksLikeGitSource()` 辅助函数 — 引入
- fragment ref 相关的类型定义（如 ParsedSource 中的 ref 字段）— 引入

**(d) 从上游引入 SSH URL 解析修复（per BUG-03）：**
- SSH URL 格式支持（`git@gitlab.com:owner/repo`）— 引入

**(e) 从上游引入 github: 协议前缀支持：**
- `github:owner/repo` 语法解析 — 引入

**步骤 4: 融合后的 parseSource() 执行流程应为：**
1. 如果是 Market 安装命令 → Market 解析（BMC 逻辑）
2. 如果是裸技能名 → Market 查询（BMC 逻辑）
3. Fragment ref 解析（上游逻辑）
4. sanitizeSubpath() 调用（上游安全修复）
5. SSH URL / github: 前缀 / 标准 GitHub 解析（上游 + BMC 混合）
6. COS URL 解析（BMC 逻辑）
7. 本地路径 / URL 回退（双方共有）

**步骤 5: 清除所有冲突标记并保存**
确保文件中没有 `<<<<<<<`, `=======`, `>>>>>>>` 标记。

**步骤 6: git add**
```bash
git add src/source-parser.ts
```

**关键验证点：**
- `grep "sanitizeSubpath" src/source-parser.ts` — SEC-01 安全函数存在
- `grep "isMarketInstallCommand\|isBareSkillName\|parseMarketInstallCommand" src/source-parser.ts` — BMC Market 逻辑保留
- `grep "#\|fragment\|ref\|branch" src/source-parser.ts` — FEAT-01 branch ref 支持
- `grep "ssh\|git@" src/source-parser.ts` — BUG-03 SSH URL 支持
- `grep "<<<<<<<" src/source-parser.ts` — 无冲突标记残留
  </action>
  <verify>
    <automated>cd "C:/Users/LY/Code/skill2/skills-cli" && grep "sanitizeSubpath" src/source-parser.ts > /dev/null && grep "isMarketInstallCommand" src/source-parser.ts > /dev/null && grep "isBareSkillName" src/source-parser.ts > /dev/null && ! grep "<<<<<<<" src/source-parser.ts && echo "source-parser.ts OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "sanitizeSubpath" src/source-parser.ts` 有输出（SEC-01 安全修复已引入）
    - `grep "isMarketInstallCommand" src/source-parser.ts` 有输出（BMC Market 逻辑保留）
    - `grep "isBareSkillName" src/source-parser.ts` 有输出（BMC Market 逻辑保留）
    - `grep "parseMarketInstallCommand" src/source-parser.ts` 有输出（BMC Market 逻辑保留）
    - `grep "<<<<<<<" src/source-parser.ts` 无输出（无冲突标记残留）
    - `git diff --name-only --diff-filter=U` 不包含 source-parser.ts
  </acceptance_criteria>
  <done>
    source-parser.ts 合并完成：包含 BMC 的 Market/COS 解析逻辑 + 上游的 sanitizeSubpath 安全修复 + branch ref 支持 + SSH URL 解析。无冲突标记残留。
  </done>
</task>

<task type="auto">
  <name>Task 2: 解决 cli.ts 高冲突合并（--json flag + update-source + BMC 品牌/Market 集成）</name>
  <files>src/cli.ts</files>
  <read_first>
    src/cli.ts,
    src/update-source.ts,
    src/branding.ts
  </read_first>
  <action>
**背景（陷阱 #1 警告）：** cli.ts 是双方改动量最大的文件。上游重构了 check/update 命令（引入 update-source.ts，移除 CheckUpdatesRequest/CheckUpdatesResponse 接口，新增 --json 输出），BMC 有大量品牌替换（NPX_CMD, BIN_NAME 等）和 Market 集成。**必须逐段审查，防止品牌名回退。**

**步骤 1: 理解当前冲突状态**
```bash
grep -c "<<<<<<<" src/cli.ts
cat src/cli.ts
```

**步骤 2: 获取三方参考版本**
```bash
git show upstream/main:src/cli.ts > /tmp/cli-upstream.ts
git show HEAD:src/cli.ts > /tmp/cli-bmc.ts 2>/dev/null
```

**步骤 3: 手动融合策略**

**(a) 保留 BMC 品牌和 Market 集成（最高优先级）：**
- 所有 `import` 语句中的 BMC 独有模块（branding.ts, find.ts 中的 Market 函数等）— 保留
- `BIN_NAME`, `NPX_CMD`, `SKILLS_SITE`, `PACKAGE_NAME` 等品牌常量引用 — 保留 BMC 版本
- Market Provider 相关 import 和调用 — 保留
- `readLocalLock` import（如果有）— 保留
- find 命令路由（BMC 接入 Market 搜索）— 保留

**(b) 从上游引入 --json flag（per FEAT-02）：**
- `list` 命令的 `--json` 参数解析 — 引入
- `parseListOptions()` 中的 json 支持（如果在 cli.ts 中）— 引入
- json 输出格式化逻辑 — 引入

**(c) 从上游引入 update-source.ts 集成（per FEAT-04 依赖）：**
- `import { buildUpdateInstallSource } from './update-source.ts'` — 引入
- check/update 命令中使用 `buildUpdateInstallSource` 的逻辑 — 引入
- **注意（陷阱 #6）：** 确保 `buildUpdateInstallSource()` 不依赖上游特有的 API 端点。如果它引用了 `skills.sh` 的 URL，需要替换为 BMC 的 `SKILLS_SITE` 常量。

**(d) 从上游引入更新失败提示改进：**
- 更新失败时指明具体技能名称的错误信息 — 引入

**(e) 从上游引入 check-updates 安全限制（per BUG-05）：**
- check/update 命令中的安全限制逻辑（如果在 cli.ts 中）— 引入

**(f) 从上游引入分叉检测修复（per BUG-06）：**
- 分叉检测相关的修复逻辑 — 引入

**步骤 4: 品牌泄露检查（关键！）**
合并完成后，搜索可能的上游品牌名泄露：
```bash
# 搜索硬编码的上游品牌名
grep -n "npx skills\b" src/cli.ts
grep -n "\"skills\"" src/cli.ts
grep -n "skills.sh" src/cli.ts
```
如果发现任何硬编码的上游品牌名（非通过 branding.ts 常量引用的），替换为 BMC 对应常量。

**步骤 5: 清除冲突标记并保存**
```bash
grep "<<<<<<<" src/cli.ts  # 应无输出
git add src/cli.ts
```
  </action>
  <verify>
    <automated>cd "C:/Users/LY/Code/skill2/skills-cli" && ! grep "<<<<<<<" src/cli.ts && grep "BIN_NAME\|NPX_CMD\|PACKAGE_NAME" src/cli.ts > /dev/null && grep "update-source\|buildUpdateInstallSource" src/cli.ts > /dev/null && echo "cli.ts OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "<<<<<<<" src/cli.ts` 无输出（无冲突标记）
    - `grep "BIN_NAME" src/cli.ts` 有输出（BMC 品牌常量保留）
    - `grep "NPX_CMD" src/cli.ts` 有输出（BMC 品牌常量保留）
    - `grep "update-source\|buildUpdateInstallSource" src/cli.ts` 有输出（FEAT-04 集成）
    - `grep -c "skills\.sh" src/cli.ts` 输出 0（无上游 URL 泄露，branding 中除外）
    - `grep "json" src/cli.ts` 有输出（FEAT-02 --json flag）
  </acceptance_criteria>
  <done>
    cli.ts 合并完成：保留 BMC 品牌常量和 Market 集成，引入上游 --json flag、update-source 集成和错误提示改进。无冲突标记残留，无上游品牌泄露。
  </done>
</task>

<task type="auto">
  <name>Task 3: 提交合并并验证工作区干净</name>
  <files></files>
  <read_first>
    src/source-parser.ts,
    src/cli.ts
  </read_first>
  <action>
**步骤 1: 最终冲突检查**
```bash
# 确认无未解决的冲突文件
git diff --name-only --diff-filter=U
# 应输出为空

# 确认无冲突标记残留（全仓库扫描）
grep -r "<<<<<<<" src/ tests/ 2>/dev/null
# 应无输出
```

**步骤 2: 检查暂存区状态**
```bash
git status
# 应显示 "All conflicts fixed but you are still merging"
# 所有文件应在 staged 状态
```

如果有遗漏的文件未 add：
```bash
git add -A
```

**步骤 3: 提交合并**
```bash
git commit --no-edit
```
这将使用默认的合并提交信息（"Merge remote-tracking branch 'upstream/main' into sync/upstream-v1.4.7"）。

**步骤 4: 验证提交成功**
```bash
# 工作区应干净
git status
# 应显示 "nothing to commit, working tree clean"

# 查看合并提交
git log --oneline -3
# 应看到合并提交在最顶部

# 确认 merge commit 有两个 parent
git log --format="%H %P" -1
# 应有两个 parent hash
```

**步骤 5: 全量 PROT 验证（最终确认）**
```bash
# PROT-01: branding.ts
grep "blueai-skills" src/branding.ts && echo "PROT-01 OK"
# PROT-02: cos.ts
test -f src/providers/cos.ts && echo "PROT-02 OK"
# PROT-03: market.ts
test -f src/providers/market.ts && echo "PROT-03 OK"
# PROT-04: telemetry.ts
grep "return false" src/telemetry.ts && echo "PROT-04 OK"
# PROT-05: find.ts
grep "SKILLS_SITE" src/find.ts && echo "PROT-05 OK"
# PROT-06: package.json
node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); if(p.name!=='blueai-skills') process.exit(1); console.log('PROT-06 OK: name=' + p.name + ' version=' + p.version)"
```
  </action>
  <verify>
    <automated>cd "C:/Users/LY/Code/skill2/skills-cli" && git status --porcelain | head -1 | grep -q "^$" || (git diff --name-only --diff-filter=U | wc -l | grep "^0$") && git log --format="%P" -1 | grep -q " " && echo "Merge commit verified"</automated>
  </verify>
  <acceptance_criteria>
    - `git status` 显示 "nothing to commit, working tree clean"
    - `git log --format="%P" -1` 有两个 parent hash（证明是 merge commit）
    - `grep -r "<<<<<<<" src/ tests/` 无输出
    - PROT-01 到 PROT-06 全部通过
  </acceptance_criteria>
  <done>
    合并提交已创建。工作区干净，无未解决冲突。合并提交有两个 parent（BMC sync 分支 + upstream/main）。所有 PROT 要求验证通过。
  </done>
</task>

</tasks>

<verification>
1. `git status` — 工作区干净
2. `git log --oneline -1` — 显示合并提交
3. PROT-01 到 PROT-06 全部验证通过
4. `grep "sanitizeSubpath" src/source-parser.ts` — SEC-01 存在
5. `grep "isMarketInstallCommand" src/source-parser.ts` — BMC Market 逻辑保留
6. `grep "BIN_NAME" src/cli.ts` — BMC 品牌保留
7. `grep "update-source" src/cli.ts` — FEAT-04 集成
8. `grep -r "skills\.sh" src/` — 排除 branding.ts 后无上游 URL 泄露
</verification>

<success_criteria>
- source-parser.ts 包含 BMC Market 逻辑 + 上游 sanitizeSubpath + branch ref 支持（SEC-01, FEAT-01, BUG-03）
- cli.ts 包含 BMC 品牌/Market 集成 + --json flag + update-source 集成（FEAT-02, BUG-05, BUG-06）
- 合并提交已创建，工作区干净
- 全仓库无冲突标记残留
- 无上游品牌名泄露
</success_criteria>

<output>
After completion, create `.planning/phases/02-selective-merge/02-02-SUMMARY.md`
</output>
