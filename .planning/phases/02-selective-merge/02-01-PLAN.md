---
phase: 02-selective-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents.ts
  - src/types.ts
  - src/installer.ts
  - src/skill-lock.ts
  - src/local-lock.ts
  - src/list.ts
  - src/add.ts
  - src/install.ts
  - src/skills.ts
  - src/git.ts
  - src/list.test.ts
  - src/init.test.ts
  - src/cli.test.ts
  - src/providers/wellknown.ts
  - src/providers/types.ts
  - src/providers/registry.ts
  - src/update-source.ts
  - src/update-source.test.ts
  - src/source-parser.test.ts
  - tests/source-parser.test.ts
  - tests/subpath-traversal.test.ts
  - tests/local-lock.test.ts
  - tests/wellknown-provider.test.ts
  - tests/xdg-config-paths.test.ts
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - BUG-01
  - BUG-02
  - BUG-03
  - BUG-04
  - BUG-05
  - BUG-06
  - FEAT-03
  - FEAT-04
  - PROT-01
  - PROT-02
  - PROT-03
  - PROT-04
  - PROT-05
  - PROT-06

must_haves:
  truths:
    - "git merge upstream/main 已执行，合并状态进行中（--no-commit）"
    - "BMC 保护文件（branding.ts, cos.ts, market.ts, index.ts, telemetry.ts, find.ts, package.json, README.md）内容与合并前完全一致"
    - "上游新 agent 定义（warp, deepagents, firebender, bob）已存在于 agents.ts"
    - "上游 update-source.ts 新模块已引入"
    - "上游安全修复（sanitizeSubpath）和 bug 修复已合并到对应文件中"
    - "所有非高冲突文件的冲突已解决"
  artifacts:
    - path: "src/agents.ts"
      provides: "新 agent 定义（warp, deepagents, firebender, bob）"
      contains: "warp"
    - path: "src/update-source.ts"
      provides: "update-source 辅助模块"
      contains: "buildUpdateInstallSource"
    - path: "src/update-source.test.ts"
      provides: "update-source 测试"
      contains: "buildUpdateInstallSource"
    - path: "tests/subpath-traversal.test.ts"
      provides: "子路径遍历防护测试"
      contains: "sanitize"
  key_links:
    - from: "src/agents.ts"
      to: "src/types.ts"
      via: "AgentType 联合类型引用"
      pattern: "AgentType"
    - from: "src/update-source.ts"
      to: "src/source-parser.ts"
      via: "parseSource 调用"
      pattern: "parseSource"
---

<objective>
执行 git merge upstream/main --no-commit，解决所有非高冲突文件的合并冲突，确保 BMC 保护文件完整保留。

Purpose: 将上游 v1.4.7 的安全修复、bug 修复和新功能引入到 sync 分支，同时确保 BMC 定制内容不被覆盖。这是合并操作的主体步骤。
Output: 合并状态中的工作区，所有简单/中等冲突文件已解决，仅剩 source-parser.ts 和 cli.ts 待下一个 plan 处理。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-merge-prep/01-01-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.gitattributes
</context>

<tasks>

<task type="auto">
  <name>Task 1: 执行 git merge 并解决保护文件 + 简单冲突</name>
  <files>
    src/branding.ts, src/providers/cos.ts, src/providers/market.ts, src/providers/index.ts,
    src/telemetry.ts, src/find.ts, package.json, README.md,
    src/agents.ts, src/types.ts, src/skills.ts, src/git.ts,
    src/install.ts, src/list.ts, src/list.test.ts,
    src/providers/wellknown.ts, src/providers/types.ts, src/providers/registry.ts,
    src/update-source.ts, src/update-source.test.ts,
    tests/subpath-traversal.test.ts, tests/local-lock.test.ts,
    tests/wellknown-provider.test.ts, tests/xdg-config-paths.test.ts,
    src/init.test.ts, src/cli.test.ts, src/source-parser.test.ts, tests/source-parser.test.ts,
    .github/*, AGENTS.md, .gitignore, ThirdPartyNoticeText.txt, skills/find-skills/SKILL.md,
    scripts/*, .planning/*
  </files>
  <read_first>
    .gitattributes,
    src/branding.ts,
    src/providers/cos.ts,
    src/providers/market.ts,
    src/providers/index.ts,
    src/telemetry.ts,
    src/find.ts,
    package.json
  </read_first>
  <action>
**重要前置操作：** 在执行合并前，先用 `cat` 或读取工具保存以下 BMC 保护文件的当前内容快照（用于合并后验证）：
- `src/branding.ts` — 记录前 5 行特征内容
- `src/providers/cos.ts` — 记录前 3 行
- `src/providers/market.ts` — 记录前 3 行
- `src/providers/index.ts` — 记录导出列表
- `src/telemetry.ts` — 记录 `return false` 行
- `src/find.ts` — 记录 SKILLS_SITE import
- `package.json` — 记录 name, version, bin 字段

**步骤 1: 确认 merge.ours.driver 配置**
```bash
git config merge.ours.driver
# 应输出: true
```
如果没有配置，执行 `git config merge.ours.driver true`。

**步骤 2: 执行合并（不自动提交）**
```bash
git merge upstream/main --no-commit --no-edit
```
这将产生合并冲突。.gitattributes 中配置了 merge=ours 的文件应自动保留 BMC 版本。

**步骤 3: 检查冲突文件列表**
```bash
git diff --name-only --diff-filter=U
```
记录所有冲突文件。

**步骤 4: 处理保护文件**
对于 .gitattributes 中的 8 个保护文件，如果它们仍然出现在冲突列表中（merge=ours 未生效），手动强制保留 BMC 版本：
```bash
git checkout --ours src/branding.ts
git checkout --ours src/providers/cos.ts
git checkout --ours src/providers/market.ts
git checkout --ours src/providers/index.ts
git checkout --ours src/telemetry.ts
git checkout --ours src/find.ts
git checkout --ours package.json
git checkout --ours README.md
git add src/branding.ts src/providers/cos.ts src/providers/market.ts src/providers/index.ts src/telemetry.ts src/find.ts package.json README.md
```

**步骤 5: 处理 BMC 独有文件（上游不应覆盖）**
这些是 BMC 添加的文件或配置，上游的 diff 可能试图删除它们：
```bash
# .planning/ 目录 — 保留 BMC 版本
git checkout --ours .planning/ 2>/dev/null; git add .planning/ 2>/dev/null
# CLAUDE.md — BMC 独有
git checkout --ours CLAUDE.md 2>/dev/null; git add CLAUDE.md 2>/dev/null
# scripts/ — BMC 独有
git checkout --ours scripts/ 2>/dev/null; git add scripts/ 2>/dev/null
# .gitattributes — BMC 独有
git checkout --ours .gitattributes 2>/dev/null; git add .gitattributes 2>/dev/null
```

**步骤 6: 处理上游新增文件（直接接受）**
以下文件是上游新增的，BMC 没有，应直接接受上游版本：
```bash
git add src/update-source.ts src/update-source.test.ts 2>/dev/null
git add tests/subpath-traversal.test.ts 2>/dev/null
git add tests/local-lock.test.ts 2>/dev/null
git add tests/xdg-config-paths.test.ts 2>/dev/null
```

**步骤 7: 处理上游恢复的文件（BMC 已删除的）**
上游的 `src/providers/registry.ts` 和 `src/providers/types.ts` 在 BMC 中已被删除。合并可能会恢复它们。**保留上游版本**，因为 `src/providers/wellknown.ts` 可能依赖它们的类型定义：
```bash
git add src/providers/registry.ts src/providers/types.ts 2>/dev/null
```
注意：如果这导致与 BMC 的 `src/providers/index.ts`（已 merge=ours 保护）有导入冲突，需要在后续调整 index.ts 的导出以兼容。

**步骤 8: 处理无 BMC 定制的文件（接受上游版本）**
```bash
# 这些文件 BMC 没有定制，直接接受上游
git checkout --theirs src/skills.ts 2>/dev/null; git add src/skills.ts
git checkout --theirs src/git.ts 2>/dev/null; git add src/git.ts 2>/dev/null
git checkout --theirs src/providers/wellknown.ts 2>/dev/null; git add src/providers/wellknown.ts 2>/dev/null
```

**步骤 9: 处理 .github/ 和其他配置文件**
```bash
# .github/ — 保留 BMC 版本（BMC 有自己的 CI/CD）
git checkout --ours .github/ 2>/dev/null; git add .github/ 2>/dev/null
# AGENTS.md — 保留 BMC 版本
git checkout --ours AGENTS.md 2>/dev/null; git add AGENTS.md 2>/dev/null
# .gitignore — 接受上游（一般无冲突）
git add .gitignore 2>/dev/null
# ThirdPartyNoticeText.txt — 接受上游
git add ThirdPartyNoticeText.txt 2>/dev/null
```

**步骤 10: 验证保护文件完整性 (per PROT-01 到 PROT-06)**
```bash
# PROT-01: branding.ts 仍包含 BMC 品牌
grep "blueai-skills" src/branding.ts
# PROT-02: cos.ts 仍存在
test -f src/providers/cos.ts && echo "cos.ts OK"
# PROT-03: market.ts 仍存在
test -f src/providers/market.ts && echo "market.ts OK"
# PROT-04: telemetry.ts 默认关闭
grep "return false" src/telemetry.ts
# PROT-05: find.ts 仍使用 SKILLS_SITE
grep "SKILLS_SITE" src/find.ts
# PROT-06: package.json name 仍为 blueai-skills
node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log(p.name); process.exit(p.name==='blueai-skills'?0:1)"
```

**注意：** 此任务完成后，source-parser.ts 和 cli.ts 的冲突暂不解决，留给 Task 2 处理。其他所有冲突文件应在此步骤全部解决。
  </action>
  <verify>
    <automated>cd "C:/Users/LY/Code/skill2/skills-cli" && grep "blueai-skills" src/branding.ts > /dev/null && grep "return false" src/telemetry.ts > /dev/null && grep "SKILLS_SITE" src/find.ts > /dev/null && node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); process.exit(p.name==='blueai-skills'?0:1)" && echo "PROT checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "blueai-skills" src/branding.ts` 有输出（PROT-01）
    - `test -f src/providers/cos.ts` 返回 0（PROT-02）
    - `test -f src/providers/market.ts` 返回 0（PROT-03）
    - `grep "return false" src/telemetry.ts` 有输出（PROT-04）
    - `grep "SKILLS_SITE" src/find.ts` 有输出（PROT-05）
    - `node -e "...p.name==='blueai-skills'..."` 返回 0（PROT-06）
    - `git diff --name-only --diff-filter=U` 输出中仅剩 src/source-parser.ts 和 src/cli.ts（或更少）
    - `test -f src/update-source.ts` 返回 0（FEAT-04）
    - `test -f tests/subpath-traversal.test.ts` 返回 0（SEC-01 测试）
  </acceptance_criteria>
  <done>
    合并已执行（--no-commit 状态）。8 个 BMC 保护文件内容与合并前完全一致。上游新增文件（update-source.ts, subpath-traversal.test.ts 等）已引入。无 BMC 定制的文件已接受上游版本。仅剩 source-parser.ts 和 cli.ts 待解决。
  </done>
</task>

<task type="auto">
  <name>Task 2: 解决中等冲突文件（agents.ts, types.ts, installer.ts, skill-lock.ts, local-lock.ts, add.ts）</name>
  <files>
    src/agents.ts, src/types.ts, src/installer.ts, src/skill-lock.ts, src/local-lock.ts, src/add.ts
  </files>
  <read_first>
    src/agents.ts,
    src/types.ts,
    src/installer.ts,
    src/skill-lock.ts,
    src/local-lock.ts,
    src/add.ts
  </read_first>
  <action>
这些文件双方都有修改，但冲突范围可控。合并策略：**在 BMC 版本基础上，手动引入上游的新增/改进代码**。

**对于每个文件，执行以下流程：**
1. 查看冲突标记 `<<<<<<<` 的位置和数量
2. 根据下方指导手动解决每处冲突
3. 解决后 `git add <file>`

---

**src/agents.ts (per FEAT-03):**
- 上游新增了 warp, deepagents, firebender, bob 四个 agent 定义
- BMC 无重大定制
- **策略：** 接受上游版本。如果有冲突标记，查看上游新增的 agent 定义块，将它们全部保留。
- **验证：** `grep -c "warp\|deepagents\|firebender\|bob" src/agents.ts` 应 >= 4
- 注意：上游可能还更新了已有 agent 的路径（如 Antigravity），也要接受

**src/types.ts (per FEAT-03 依赖):**
- 上游新增了 AgentType 联合类型成员（对应新 agent）
- BMC 新增了 ParsedSource 的 `market` 类型和相关字段
- **策略：** 保留 BMC 的 market 相关类型扩展，同时合入上游新增的 AgentType 成员
- **关键检查：** ParsedSource 类型中 `type` 字段的联合类型应包含 `'market'`（BMC 独有）和上游所有类型
- **验证：** `grep "market" src/types.ts` 应有输出（BMC 定制保留）

**src/installer.ts (per BUG-02, BUG-05):**
- 上游修复了：跳过损坏 symlink（BUG-02）、停止排除下划线前缀文件、共享目录时跳过提示
- BMC 可能有微调
- **策略：** 优先接受上游的 bug 修复。检查冲突区域，如果 BMC 只有微小改动（如注释、格式），接受上游版本。如果 BMC 有实质性逻辑修改，手动融合。
- **验证：** `grep -c "symlink\|broken" src/installer.ts` 应有匹配（上游修复代码存在）

**src/skill-lock.ts (per BUG-04):**
- 上游引入了 XDG_STATE_HOME 支持（避免硬编码 ~/.agents 路径）
- BMC 可能有 authorId 相关修改
- **策略：** 接受上游的 XDG 路径改进，保留 BMC 的 authorId 字段逻辑
- **注意（陷阱 #5）：** XDG 路径改动是语义变更。确保 BMC 的 authorId 写入逻辑在新路径下仍然正确工作。
- **验证：** `grep "XDG_STATE_HOME\|xdgStatePath\|authorId" src/skill-lock.ts` 应同时有 XDG 和 authorId

**src/local-lock.ts (per BUG-06):**
- BMC 新增了 authorId 字段支持
- 上游可能有其他小改动
- **策略：** 保留 BMC 的 authorId 逻辑，合入上游其他改进
- **验证：** `grep "authorId" src/local-lock.ts` 应有输出

**src/add.ts (per SEC-02, BUG-01, BUG-02 依赖):**
- BMC 有 COS/Market provider 集成（import cosProvider, marketProvider）
- 上游有 auth token 传递修复（SEC-02）、symlink 提示优化
- **策略：** 保留 BMC 的所有 provider 集成代码（import 语句、Market 安装流程）。从上游引入 auth token 传递逻辑和 UX 改进。
- **关键检查：** 确保以下 BMC import 仍存在：
  - `import { cosProvider } from './providers/cos.ts'` 或类似
  - `import { marketProvider } from './providers/market.ts'` 或类似
- **验证：** `grep "cosProvider\|marketProvider" src/add.ts` 应有输出

**解决所有冲突后：**
```bash
git add src/agents.ts src/types.ts src/installer.ts src/skill-lock.ts src/local-lock.ts src/add.ts
```
  </action>
  <verify>
    <automated>cd "C:/Users/LY/Code/skill2/skills-cli" && grep -c "warp\|deepagents\|firebender\|bob" src/agents.ts && grep "market" src/types.ts > /dev/null && grep "authorId" src/local-lock.ts > /dev/null && echo "Medium conflict files resolved"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "warp" src/agents.ts` 有输出（FEAT-03 新 agent）
    - `grep "deepagents" src/agents.ts` 有输出（FEAT-03 新 agent）
    - `grep "firebender" src/agents.ts` 有输出（FEAT-03 新 agent）
    - `grep "bob" src/agents.ts` 有输出（FEAT-03 新 agent）
    - `grep "market" src/types.ts` 有输出（BMC market 类型保留）
    - `grep "authorId" src/local-lock.ts` 有输出（BMC authorId 保留）
    - `grep "cosProvider\|marketProvider" src/add.ts` 有输出（BMC provider 保留）
    - 6 个文件都已 `git add`，不再出现在 `git diff --name-only --diff-filter=U`
    - 无 `<<<<<<<` 冲突标记残留：`grep -r "<<<<<<<" src/agents.ts src/types.ts src/installer.ts src/skill-lock.ts src/local-lock.ts src/add.ts` 无输出
  </acceptance_criteria>
  <done>
    agents.ts 包含 4 个新 agent 定义。types.ts 保留 BMC market 类型且包含新 AgentType。installer.ts 包含上游 bug 修复。skill-lock.ts 有 XDG 支持且保留 authorId。local-lock.ts 保留 authorId。add.ts 保留 BMC provider 集成且引入 auth token 修复。所有 6 个文件无冲突标记残留。
  </done>
</task>

</tasks>

<verification>
1. `git diff --name-only --diff-filter=U` — 仅剩 source-parser.ts 和 cli.ts（或更少）
2. 保护文件验证（PROT-01 到 PROT-06）全部通过
3. `grep -r "<<<<<<<" src/ tests/` — 除 source-parser.ts 和 cli.ts 外无冲突标记
4. `test -f src/update-source.ts` — 上游新文件已引入
5. `grep "warp" src/agents.ts` — 新 agent 定义存在
</verification>

<success_criteria>
- 合并状态中（git merge --no-commit），除 source-parser.ts 和 cli.ts 外所有冲突已解决
- 8 个 BMC 保护文件内容与合并前完全一致（PROT-01 到 PROT-06）
- 上游新增文件（update-source.ts, subpath-traversal.test.ts 等）已引入（FEAT-04, SEC-01 部分）
- agents.ts 包含 warp/deepagents/firebender/bob（FEAT-03）
- installer.ts 包含 symlink 修复（BUG-02）和下划线文件修复（BUG-05）
- skill-lock.ts 包含 XDG 路径支持（BUG-04）
- add.ts 保留 BMC provider 集成且引入 auth token 修复（SEC-02, BUG-01）
</success_criteria>

<output>
After completion, create `.planning/phases/02-selective-merge/02-01-SUMMARY.md`
</output>
