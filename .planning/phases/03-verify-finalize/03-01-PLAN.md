---
phase: 03-verify-finalize
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - VAL-01
  - VAL-02
  - VAL-03
  - VAL-04

must_haves:
  truths:
    - "pnpm build 零错误完成"
    - "pnpm test 全部测试通过"
    - "BMC 定制功能（Market API、COS provider、find 命令）代码路径完整"
    - "代码中无 skills.sh 或上游品牌名硬编码泄露"
  artifacts:
    - path: "dist/"
      provides: "构建产物"
      contains: "cli.js"
  key_links: []
---

<objective>
验证合并后的代码完整性：构建通过、测试通过、BMC 定制功能完整、无品牌泄露。

Purpose: 确保 Phase 2 的合并没有破坏任何功能。
Output: 构建和测试全部通过，项目可发布。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 构建验证（VAL-01）</name>
  <files></files>
  <read_first>
    package.json
  </read_first>
  <action>
**步骤 1: 运行构建**
```bash
pnpm build
```

**步骤 2: 如果构建失败，分析错误并修复**
- TypeScript 类型错误：检查上游引入的新类型是否与 BMC 定制兼容
- 缺失导入：检查 update-source.ts、registry.ts 等新文件的导入路径
- 修复后重新构建直到零错误

**步骤 3: 验证构建产物**
```bash
ls dist/
# 应包含 cli.js 等构建产物
```
  </action>
  <verify>
    <automated>cd "C:/Users/LY/Code/skill2/skills-cli" && pnpm build 2>&1 && test -f dist/cli.js && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` 退出码 0
    - `dist/cli.js` 文件存在
  </acceptance_criteria>
  <done>
    pnpm build 零错误完成，构建产物已生成。
  </done>
</task>

<task type="auto">
  <name>Task 2: 测试验证（VAL-02）</name>
  <files></files>
  <read_first>
    package.json
  </read_first>
  <action>
**步骤 1: 运行测试**
```bash
pnpm test
```

**步骤 2: 如果测试失败，分析并修复**
- 上游新增测试可能引用上游特有的常量/路径，需要适配 BMC
- BMC 现有测试如果因为上游代码变更失败，需要更新断言
- 修复后重新测试直到全部通过

**步骤 3: 确认测试结果**
全部测试通过。
  </action>
  <verify>
    <automated>cd "C:/Users/LY/Code/skill2/skills-cli" && pnpm test 2>&1 && echo "TESTS OK"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` 退出码 0
    - 无失败测试
  </acceptance_criteria>
  <done>
    pnpm test 全部测试通过。
  </done>
</task>

<task type="auto">
  <name>Task 3: BMC 定制功能冒烟测试 + 品牌检查（VAL-03, VAL-04）</name>
  <files></files>
  <read_first>
    src/branding.ts,
    src/find.ts
  </read_first>
  <action>
**步骤 1: 验证 BMC Market API 代码路径（VAL-03）**
```bash
# find 命令使用 BMC Market API
grep "SEARCH_API_BASE\|searchSkillsAPI\|SKILLS_SITE" src/find.ts
# 应有输出

# COS provider 存在且功能完整
grep "cosProvider\|CosProvider" src/providers/cos.ts
# 应有输出

# Market provider 存在且功能完整
grep "marketProvider\|MarketProvider" src/providers/market.ts
# 应有输出
```

**步骤 2: 品牌泄露全量检查（VAL-04）**
```bash
# 搜索上游品牌名硬编码（排除 branding.ts 和测试文件）
grep -rn "skills\.sh" src/ --include="*.ts" | grep -v "branding.ts" | grep -v "test"
# 应无输出

# 搜索上游包名硬编码
grep -rn "\"skills\"" src/ --include="*.ts" | grep -v "branding.ts" | grep -v "test" | grep -v "blueai-skills" | grep -v "skill" | grep -v "import"
# 检查是否有问题

# 搜索上游 npx 命令硬编码
grep -rn "npx skills\b" src/ --include="*.ts" | grep -v "branding.ts"
# 应无输出
```

**步骤 3: 如果发现品牌泄露，修复**
将硬编码的上游品牌名替换为 BMC 对应的 branding.ts 常量。
  </action>
  <verify>
    <automated>cd "C:/Users/LY/Code/skill2/skills-cli" && grep "SEARCH_API_BASE" src/find.ts > /dev/null && grep "cosProvider\|CosProvider" src/providers/cos.ts > /dev/null && ! grep -rn "skills\.sh" src/ --include="*.ts" | grep -v "branding.ts" | grep -v test && echo "SMOKE TEST + BRAND CHECK OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "SEARCH_API_BASE" src/find.ts` 有输出（Market API 路径完整）
    - `grep "cosProvider" src/providers/cos.ts` 有输出（COS provider 完整）
    - `grep -rn "skills.sh" src/ | grep -v branding.ts | grep -v test` 无输出（无品牌泄露）
  </acceptance_criteria>
  <done>
    BMC 定制功能代码路径完整，无上游品牌名泄露。
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` — 零错误
2. `pnpm test` — 全部通过
3. BMC Market/COS/find 代码路径完整
4. 无 skills.sh 品牌泄露
</verification>

<success_criteria>
- pnpm build 通过
- pnpm test 通过
- BMC 功能冒烟测试通过
- 品牌检查通过
</success_criteria>

<output>
After completion, create `.planning/phases/03-verify-finalize/03-01-SUMMARY.md`
</output>
